## 2024-05-24 - CustomTkinter Button Hover States
**Learning:** CustomTkinter buttons do not show a pointer cursor on hover by default, leading to poor interaction feedback.
**Action:** Always explicitly set `cursor="hand2"` on interactive components like `ActionButton` to provide visual hover feedback.
